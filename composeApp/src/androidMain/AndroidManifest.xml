<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Least-privilege set: the app reads torrents via SAF (no storage
         permissions), runs no foreground service and only needs network +
         notifications. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- usesCleartextTraffic: explicitly allowed because BitTorrent HTTP(S)
         trackers and HTTP RSS feeds are a core, user-requested feature.
         Peer/Tracker traffic itself runs in the Rust engine (raw sockets). -->
    <application
        android:name="com.typebit.TypeBitApplication"
        android:label="TypeBitTorrent"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.TypeBitTorrent"
        android:allowBackup="false"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true">
        <activity
            android:name="com.typebit.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
